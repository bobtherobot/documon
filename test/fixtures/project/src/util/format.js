/**
 * String helpers used across the library.
 *
 * @module  format
 * @package util
 */

/**
 * Trims and lowercases a string.
 *
 * @method  slug
 * @param   {string} text - The input.
 * @return  {string}      - The slug.
 */
function slug(text){}

/**
 * The separator used between slug words.
 *
 * @property {string} separator="-"
 * @readonly
 */
var separator = "-";

# __proto__

A file named `__proto__.md` beside a folder named `__proto__`. Both clean to the same id,
so this page becomes the **index page** for that section rather than a second page: the
menu shows it as a normal entry that also has children.

Links to [undefined](more.__proto__.undefined), which is the child inside the folder.

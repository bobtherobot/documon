/**
 * A well-formed fixture module.
 *
 * @module  good
 * @package fixture
 */

/**
 * Adds two numbers.
 *
 * @method  add
 * @param   {number}  a - First addend.
 * @param   {number}  b - Second addend.
 * @return  {number}    - The sum.
 */
function add(a, b){ return a + b; }

/**
 * The running total.
 *
 * @property {number} total=0
 */
var total = 0;
